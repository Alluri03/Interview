# React Exercise — Search & Pagination (15–20 min)

**Goal:** Implement a debounced search, client-side pagination (5 per page), and highlight the search term in names.

Run:
```bash
npm install
npm run dev   # to run app
npm test      # to run tests
```

Work in `src/App.jsx`. Look for TODOs.
